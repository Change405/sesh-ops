# ============================================================================
# Terraform
# ============================================================================

# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Ignore tfplan files (plan output of command: terraform plan -out=tfplan)
*.tfplan
*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# ============================================================================
# Ansible
# ============================================================================

# Retry files created when playbook execution fails
*.retry
ansible/*.retry

# Ansible cache and temporary files
.ansible/
.ansible_cache/

# Vault password files (should never be committed)
vault-password.txt
.vault_pass
.vault_password
*vault-pass*

# Local inventory files that may contain sensitive host information
inventory/local
hosts.local
ansible/inventory/hosts.local.yml

# Deployment detail files (contain IP addresses and configuration)
ansible/deployment-*.txt

# Ansible collections directory (if using requirements.yml, these are downloaded)
collections/

# Ansible roles directory (if using requirements.yml, these are downloaded)
roles/*/
!roles/.gitkeep

# ============================================================================
# Miscellaneous
# ============================================================================

# JetBrains IDEs (IntelliJ, PyCharm, etc.)
.idea/
*.iml
*.iws
*.ipr

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# macOS thumbnails
._*

# macOS directories
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# VS Code
.vscode/
*.code-workspace

# Vim
*.swp
*.swo
*~
.*.swp
