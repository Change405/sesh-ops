---
# Ansible Inventory Template for Session Service Nodes
#
# This is an EXAMPLE template. DO NOT edit this file directly.
# The actual inventory is generated automatically from Terraform outputs.
#
# To generate the real inventory, run:
#   cd ansible
#   ./scripts/generate-inventory.sh
#
# This will create inventory/hosts.local (which is gitignored)

all:
  children:
    session_nodes:
      hosts:
        # Example format (automatically generated):
        #
        # session-service-nodes-node-1:
        #   ansible_host: 203.0.113.10
        #   node_public_ip: 203.0.113.10
        #
        # session-service-nodes-node-2:
        #   ansible_host: 203.0.113.20
        #   node_public_ip: 203.0.113.20

      # Group variables can be defined here or in group_vars/
      # vars:
      #   example_var: value
